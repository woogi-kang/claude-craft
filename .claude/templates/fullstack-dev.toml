[team]
name = "fullstack-dev"
description = "Full-stack: backend API + frontend UI + tester"

[[agents]]
name = "Backend"
role = "API 엔드포인트 구현"
task_template = """
{goal}에 대한 백엔드 API를 구현하세요.

## 기술 스택
- FastAPI + SQLAlchemy
- Clean Architecture 패턴
- Pydantic 모델로 요청/응답 정의

## 지침
- API 엔드포인트와 비즈니스 로직에 집중
- 테스트는 별도 워커가 담당하므로 작성하지 마세요
- src/api/ 또는 src/backend/ 디렉토리만 수정
"""

[[agents]]
name = "Frontend"
role = "UI 컴포넌트 구현"
blocked_by = ["Backend"]
task_template = """
{goal}에 대한 프론트엔드 UI를 구현하세요.

## 기술 스택
- Next.js App Router (서버 컴포넌트 기본)
- TanStack Query (서버 상태)
- shadcn/ui (컴포넌트)
- Tailwind CSS (스타일링)

## 지침
- Backend 워커가 만든 API 엔드포인트에 연동
- handoff.md에서 API 스펙을 확인하세요
- src/app/ 또는 src/frontend/ 디렉토리만 수정
"""

[[agents]]
name = "Tester"
role = "테스트 작성 및 실행"
blocked_by = ["Backend", "Frontend"]
task_template = """
{goal}에 대한 테스트를 작성하고 실행하세요.

## 테스트 유형
- 단위 테스트 (핵심 로직)
- 통합 테스트 (API 엔드포인트)
- E2E 테스트 (주요 사용자 플로우)

## 지침
- pytest (백엔드) + vitest (프론트엔드)
- 선행 워커들의 handoff.md를 참고하여 테스트 대상 파악
- tests/ 디렉토리만 수정
"""
